Orders service (MongoDB)

Endpoints:
- POST /orders
- GET  /orders
- GET  /orders/:id
- PATCH /orders/:id/status

Notes:
- Implementar modelo Mongoose para pedidos e lógica de consistência.
- Configure `MONGO_URL` env var.
